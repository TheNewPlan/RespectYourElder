<%= form_for(car) do |f| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :car_make %>
    <%= f.text_field :car_make %>
  </div>

  <div class="field">
    <%= f.label :car_model %>
    <%= f.text_field :car_model %>
  </div>

  <div class="field">
    <%= f.label :car_year %>
    <%= f.date_select :car_year, :discard_month => true, :discard_day => true, :start_year => 1916 %>
  </div>

  <div class="field">
    <%= f.label :car_price %>
    <%= f.text_field :car_price %>
  </div>

  <div class="field">
    <%= f.label :car_mileage %>
    <%= f.text_field :car_mileage %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
